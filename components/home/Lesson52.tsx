import { styles } from "@/app/(tabs)/index.styles";
import { Text, View } from "react-native";

export function Lesson52() {
  return (
    <View style={styles.lesson}>
      <Text style={styles.lessonTitle}>52차시 - 앱 안에서 웹페이지 띄우기</Text>
    </View>
  );
}
