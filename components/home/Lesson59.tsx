import { styles } from "@/app/(tabs)/index.styles";
import { Text, View } from "react-native";

export function Lesson59() {
  return (
    <View style={styles.lesson}>
      <Text style={styles.lessonTitle}>59차시 - 실제 휴대폰에서 성능·API·빌드 오류 디버깅하기</Text>
    </View>
  );
}
