import { styles } from "@/app/(tabs)/index.styles";
import { Text, View } from "react-native";

export function Lesson56() {
  return (
    <View style={styles.lesson}>
      <Text style={styles.lessonTitle}>56차시 - Android와 iOS의 권한·설정·UI 차이 처리하기</Text>
    </View>
  );
}
