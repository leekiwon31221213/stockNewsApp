import { styles } from "@/app/(tabs)/index.styles";
import { Text, View } from "react-native";

export function Lesson54() {
  return (
    <View style={styles.lesson}>
      <Text style={styles.lessonTitle}>54차시 - Google·Apple·Kakao 같은 소셜 로그인 연결하기</Text>
    </View>
  );
}
