import { styles } from "@/app/(tabs)/index.styles";
import { Text, View } from "react-native";

export function Lesson60() {
  return (
    <View style={styles.lesson}>
      <Text style={styles.lessonTitle}>60차시 - 실제 React Native 앱 처음부터 완성하고 배포하기</Text>
    </View>
  );
}
