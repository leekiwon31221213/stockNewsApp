import { styles } from "@/app/(tabs)/index.styles";
import { Text, View } from "react-native";

export function Lesson50() {
  return (
    <View style={styles.lesson}>
      <Text style={styles.lessonTitle}>50차시 - 화면 진입·퇴장과 레이아웃 변경 애니메이션 만들기</Text>
    </View>
  );
}
